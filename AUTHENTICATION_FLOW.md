# 🔐 Authentication Flow Diagrams

## 📱 Phone Authentication Flow

```
┌─────────────┐
│   User      │
│  Browser    │
└──────┬──────┘
       │
       │ 1. Visits /phone-login
       ▼
┌──────────────────────────────────┐
│    PhoneAuth Component           │
│  - Input: +923001234567          │
│  - Invisible reCAPTCHA loads     │
└──────┬───────────────────────────┘
       │
       │ 2. Clicks "Send OTP"
       ▼
┌──────────────────────────────────┐
│   Firebase Authentication        │
│   signInWithPhoneNumber()        │
│  - Verifies reCAPTCHA            │
│  - Sends SMS with OTP            │
└──────┬───────────────────────────┘
       │
       │ 3. SMS with OTP (123456)
       ▼
┌──────────────────────────────────┐
│    User's Phone                  │
│    "Your code is: 123456"        │
└──────┬───────────────────────────┘
       │
       │ 4. User enters OTP
       ▼
┌──────────────────────────────────┐
│    PhoneAuth Component           │
│    confirmationResult.confirm()  │
└──────┬───────────────────────────┘
       │
       │ 5. OTP verified
       ▼
┌──────────────────────────────────┐
│    Firebase Authentication       │
│    Returns:                      │
│    - Firebase User Object        │
│    - Firebase ID Token           │
└──────┬───────────────────────────┘
       │
       │ 6. getIdToken()
       ▼
┌──────────────────────────────────┐
│    PhoneAuth Component           │
│    POST /api/auth/firebase-login │
│    Body: { idToken, phoneNumber }│
└──────┬───────────────────────────┘
       │
       │ 7. HTTP Request
       ▼
┌──────────────────────────────────┐
│    Backend Server                │
│    authController.firebaseLogin()│
└──────┬───────────────────────────┘
       │
       │ 8. Verify ID Token
       ▼
┌──────────────────────────────────┐
│    Firebase Admin SDK            │
│    verifyIdToken(idToken)        │
│    Returns: decodedToken         │
└──────┬───────────────────────────┘
       │
       │ 9. Token Valid
       ▼
┌──────────────────────────────────┐
│    MongoDB Database              │
│    Find user by phoneNumber      │
│    or firebaseUid                │
└──────┬───────────────────────────┘
       │
       ├──────────────┬──────────────┐
       │              │              │
    Found         Not Found      
       │              │              
       ▼              ▼              
  Update User    Create New User    
       │              │              
       │   ┌──────────┘              
       │   │                         
       ▼   ▼                         
┌──────────────────────────────────┐
│    User Document in MongoDB      │
│    {                             │
│      phoneNumber: +923001234567  │
│      firebaseUid: "xyz123"       │
│      isPhoneVerified: true       │
│      name: "User_4567"           │
│      role: "caregiver"           │
│    }                             │
└──────┬───────────────────────────┘
       │
       │ 10. Generate JWT
       ▼
┌──────────────────────────────────┐
│    JWT Token Generator           │
│    jwt.sign({ id: user._id })    │
│    Expiry: 7 days                │
└──────┬───────────────────────────┘
       │
       │ 11. Return Response
       ▼
┌──────────────────────────────────┐
│    HTTP Response                 │
│    {                             │
│      success: true,              │
│      data: {                     │
│        token: "jwt_token",       │
│        user: { ... }             │
│      }                           │
│    }                             │
└──────┬───────────────────────────┘
       │
       │ 12. Store token
       ▼
┌──────────────────────────────────┐
│    AuthContext                   │
│    - localStorage.setItem()      │
│    - Set user state              │
└──────┬───────────────────────────┘
       │
       │ 13. Redirect
       ▼
┌──────────────────────────────────┐
│    Dashboard / Admin Panel       │
│    User is authenticated! ✓      │
└──────────────────────────────────┘
```

---

## 🔄 New User vs Existing User Flow

### 🆕 New User (First Time)

```
User Phone: +923001234567
                │
                ▼
        Does user exist?
                │
                NO
                │
                ▼
        ┌───────────────────┐
        │  Create New User  │
        │                   │
        │  Auto-generated:  │
        │  - name           │
        │  - default role   │
        │  - verified=true  │
        │                   │
        │  From Firebase:   │
        │  - phoneNumber    │
        │  - firebaseUid    │
        └───────┬───────────┘
                │
                ▼
        Generate JWT Token
                │
                ▼
        Return to Frontend
                │
                ▼
        Welcome to AutiSmart!
        (Show profile completion)
```

### 🔁 Existing User (Returning)

```
User Phone: +923001234567
                │
                ▼
        Does user exist?
                │
                YES
                │
                ▼
        ┌───────────────────┐
        │  Update User      │
        │                   │
        │  Set:             │
        │  isPhoneVerified  │
        │  = true           │
        │                   │
        │  Link:            │
        │  firebaseUid      │
        │  (if missing)     │
        └───────┬───────────┘
                │
                ▼
        Generate JWT Token
                │
                ▼
        Return to Frontend
                │
                ▼
        Welcome back!
        (Load user dashboard)
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│  Layer 1: reCAPTCHA                     │
│  - Prevents bot attacks                 │
│  - Invisible (automatic)                │
│  - Required before OTP send             │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Layer 2: Firebase Phone Verification   │
│  - Real SMS to real phone               │
│  - OTP expires in 60 seconds            │
│  - One-time use only                    │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Layer 3: Firebase ID Token             │
│  - Cryptographically signed             │
│  - Cannot be forged                     │
│  - Short expiration time                │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Layer 4: Backend Token Verification    │
│  - Firebase Admin SDK validates         │
│  - Checks token authenticity            │
│  - Extracts verified phone number       │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Layer 5: JWT Session Token             │
│  - Secure session management            │
│  - 7-day expiration                     │
│  - Used for all API requests            │
└─────────────────────────────────────────┘
```

---

## 📊 Data Flow: Frontend → Backend → Database

```
┌─────────────────────────────────────────────────────────┐
│                     FRONTEND                            │
├─────────────────────────────────────────────────────────┤
│  PhoneAuth.jsx                                          │
│  ├─ User Input: +923001234567                          │
│  ├─ Firebase Client: Send OTP                          │
│  ├─ User Input: 123456                                 │
│  ├─ Firebase Client: Verify OTP                        │
│  └─ Get ID Token: eyJhbGciOiJSUzI1NiI...                │
└────────────────────────┬────────────────────────────────┘
                         │
         POST /api/auth/firebase-login
         { idToken, phoneNumber }
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                     BACKEND                             │
├─────────────────────────────────────────────────────────┤
│  authController.firebaseLogin()                         │
│  ├─ Receive: idToken                                   │
│  ├─ Firebase Admin: Verify Token                       │
│  ├─ Extract: firebaseUid, phoneNumber                  │
│  ├─ Query Database: Find/Create User                   │
│  ├─ Generate: JWT Token                                │
│  └─ Return: { token, user }                            │
└────────────────────────┬────────────────────────────────┘
                         │
         MongoDB Query
         findOne({ phoneNumber })
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                    DATABASE                             │
├─────────────────────────────────────────────────────────┤
│  users Collection                                       │
│  {                                                      │
│    _id: ObjectId("..."),                               │
│    name: "User_4567",                                  │
│    phoneNumber: "+923001234567",                       │
│    firebaseUid: "xyz123abc",                           │
│    isPhoneVerified: true,                              │
│    isVerified: true,                                   │
│    role: "caregiver",                                  │
│    createdAt: ISODate("..."),                          │
│    updatedAt: ISODate("...")                           │
│  }                                                      │
└────────────────────────┬────────────────────────────────┘
                         │
         Return User Document
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                     BACKEND                             │
│  Generate JWT & Send Response                           │
└────────────────────────┬────────────────────────────────┘
                         │
         Response:
         { success: true, data: { token, user } }
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                     FRONTEND                            │
│  Store token in AuthContext & localStorage              │
│  Redirect to Dashboard                                  │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 File Dependencies Map

```
BACKEND
├── server.js
│   └── routes/authRoutes.js
│       └── controllers/authController.js
│           ├── config/firebaseAdmin.js
│           │   └── serviceAccountKey.json
│           ├── models/User.js
│           └── jsonwebtoken

FRONTEND
├── main.jsx
│   └── views/App.jsx
│       └── components/Auth/PhoneAuth.jsx
│           ├── config/firebaseClient.js
│           │   └── firebase (npm package)
│           ├── context/AuthContext.jsx
│           └── axios (API calls)
```

---

## 🚦 Error Handling Flow

```
                User Action
                     │
                     ▼
            ┌────────────────┐
            │  Try Operation │
            └────────┬───────┘
                     │
         ┌───────────┴───────────┐
         │                       │
      Success                  Error
         │                       │
         ▼                       ▼
    Continue Flow      ┌─────────────────┐
                       │  Catch Error    │
                       └────────┬────────┘
                                │
                     ┌──────────┴──────────┐
                     │                     │
              Firebase Error        Backend Error
                     │                     │
                     ▼                     ▼
         ┌─────────────────────┐  ┌──────────────────┐
         │ - invalid-phone     │  │ - Server error   │
         │ - code-expired      │  │ - Network error  │
         │ - too-many-requests │  │ - Invalid token  │
         └──────────┬──────────┘  └─────────┬────────┘
                    │                        │
                    └────────┬───────────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Display User-Friendly│
                  │ Error Message        │
                  └──────────────────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Provide Action:      │
                  │ - Retry              │
                  │ - Change Phone       │
                  │ - Contact Support    │
                  └──────────────────────┘
```

---

## 🔄 OTP Lifecycle

```
Time: 0s
│
├─ User clicks "Send OTP"
│  └─ Firebase generates OTP
│     └─ SMS sent to phone
│
Time: 1-5s
│
├─ User receives SMS
│  └─ User reads OTP (123456)
│
Time: 5-30s
│
├─ User enters OTP
│  └─ User clicks "Verify"
│
Time: 30-60s
│
├─ Frontend validates with Firebase
│  ├─ Valid? → Continue to backend
│  └─ Invalid? → Show error
│
Time: >60s
│
└─ OTP Expired
   └─ User must resend OTP
      (60-second cooldown timer)
```

---

## 📱 Multi-Platform Flow

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   Desktop   │      │   Mobile    │      │   Tablet    │
│   Browser   │      │   Browser   │      │   Browser   │
└──────┬──────┘      └──────┬──────┘      └──────┬──────┘
       │                    │                    │
       └────────────┬───────┴────────────────────┘
                    │
                    ▼
         Same PhoneAuth Component
         Same Firebase Configuration
         Same Backend API
                    │
       ┌────────────┴───────────────┐
       │                            │
       ▼                            ▼
  Responsive UI              Consistent UX
  Bootstrap Grid             Same Validation
  Mobile-Optimized           Same Security
```

---

## 🎨 Component Interaction

```
App.jsx
  │
  ├─ AuthProvider (Context)
  │   ├─ login()
  │   ├─ logout()
  │   └─ isAuthenticated
  │
  ├─ ThemeProvider (Context)
  │
  └─ Routes
      │
      ├─ /phone-login
      │   └─ PhoneAuth Component
      │       ├─ useState (phoneNumber, otp, loading, error)
      │       ├─ useNavigate (redirect)
      │       ├─ useAuth (login function)
      │       ├─ Firebase (auth)
      │       └─ axios (API calls)
      │
      ├─ /login
      │   └─ Login Component
      │       └─ Link to /phone-login
      │
      └─ /dashboard (Protected)
          └─ Dashboard Component
              └─ Requires JWT token
```

---

**Visual guide for AutiSmart Phone Authentication** 📱✨
